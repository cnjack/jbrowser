apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-control
spec:
  selector:
    app: {{ .Release.Name }}-control
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.control.port }}

